<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onemm.business.mapper.real.ReportMapper">

    <insert id="addReport" parameterType="Report" useGeneratedKeys="true" keyProperty="id">
        insert into onemm.report(review_id, user_id)
        values (#{reviewId}, #{userId})
    </insert>
</mapper>